fail_fast: false
repos:
- repo: local
  hooks:
  - id: clang-format
    name: clang-format
    entry: clang-format
    language: system
    files: \.(cpp|h|hpp|cxx)$
    args:
    - --style=file
    - --fallback-style=Google
